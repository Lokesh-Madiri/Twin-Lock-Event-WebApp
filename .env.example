# ── Frontend (Vercel) ────────────────────────────────────────────
# Set this in Vercel project settings → Environment Variables
VITE_API_URL=https://your-render-app.onrender.com

# ── Backend (Render) — set in Render dashboard, NOT here ─────────
# PORT=8080                      # Render sets this automatically
# SECRET_SALT=<random-32-chars>  # Used to derive team access keys
# ADMIN_KEY=<strong-secret>      # Protects the admin panel
# CORS_ORIGIN=https://your-vercel-app.vercel.app
# FORM_LINK_NODE1=https://forms.gle/...
# FORM_LINK_NODE2=https://forms.gle/...
# LOG_LEVEL=INFO
